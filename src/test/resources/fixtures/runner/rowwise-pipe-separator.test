# Row-wise expected results with query-head separator | (bare text, no quote shell).
statement ok
CREATE TABLE rw(a INTEGER, b INTEGER, c TEXT)

statement ok
INSERT INTO rw VALUES(1, 1, 'hello world')

query IIT nosort separator |
SELECT a, b, c FROM rw
----
1 | 1 | hello world
