# Minimal row-wise expected results (default space separator via exact ----).
statement ok
CREATE TABLE rw(a INTEGER, b INTEGER, c INTEGER)

statement ok
INSERT INTO rw VALUES(1, 2, 3)

query III nosort
SELECT a, b, c FROM rw
----
1 2 3
