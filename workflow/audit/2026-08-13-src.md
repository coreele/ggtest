# 代码质量审计报告

- 日期：2026-08-13
- 执行：code-audit skill（只读，未改 `src/`）
- 基准上一次审计：2026-08-11 第二轮（`workflow/audit/2026-08-11-src-r2.md`）

## 范围
- 代码：`src/main/java` 51 个生产源文件（cli 19 / db 10 / model 11 / normalize 5 / parser 2 / runner 4）
- 标准：`workflow/agents/standards/code-audit.md`（无 `code-audit-<topic>.md` 专项）
- 登记册：`workflow/audit/register.md`（16 条；CA-001~CA-006、CA-010、CA-015、CA-016 resolved；CA-007、CA-008 accepted；CA-009、CA-011~CA-014 open）
- 自上次审计后的重大变更：MySQL 引擎支持（`MySqlJdbcExecutor` / `MySqlSchemaIsolation`）、`--parallel` 多文件并发（`ParallelExecutor` / `CliSession.executeParallel`）、`TestFileCollector`、`--override` 与 `--parallel` 互斥校验；CA-015（死代码适配器）已删除、CA-016（statement error + conn=）已修复

## 摘要
- Findings — Critical: 0 | High: 0 | Medium: 4 | Low: 5 | Info: 2
- 关注项 — Known Issue: 0 | Tech Debt: 4 | 优化: 3
- 整体质量良好。上次审计的 CA-015 / CA-016 均已修复并确认。分层与资源管理依旧规范；新增 MySQL / `--parallel` 实现质量高（`AbstractJdbcExecutor` 配置式子类正确、并发派发在主线程串行打印、`Files.walk` 与连接关闭均用 try-with-resources；`SqlLogicTestParser` 无实例可变状态，跨线程共享安全）。新发现 4 个 Medium：`--override` 原子移动回退为死代码、schema 隔离 `setSearchPath` 缺标识符校验、CLI 拒绝以 `-` 开头的值、`Main` 缺顶层兜底异常映射。

## 上次审计跟踪

| 违规/关注 | 状态 | 验证 |
|---|---|---|
| CA-015 死代码适配器 (M) | **resolved** | `EngineAdapter` / `SqliteAdapter` / `PostgresAdapter` 已从仓库删除（文件枚举无匹配） |
| CA-016 statement error + conn= (M) | **resolved** | `SqlLogicTestParser.parseStatement` 改用 `firstAttrIndex` 定位属性边界（`SqlLogicTestParser.java:149-200`），`expectedErrorMsg` 由 `tokens[2..msgEnd]` 构建，`conn=` 不再污染消息 |

## Findings（违规）

| 级别 | 标准 | 位置 | 问题 | 建议 | 修复跟踪 |
|---|---|---|---|---|---|
| Medium | §5 / §2 | `cli/OverrideWriter.java:84-88` | `writeAtomically` 的原子移动回退是死代码。内层 `catch (UnsupportedOperationException ex)`（L86）捕获的类型错误：`Files.move(src, target, ATOMIC_MOVE)` 在不支持原子移动时抛 `java.nio.file.AtomicMoveNotSupportedException`（继承 `FileSystemException → IOException`，**非** `UnsupportedOperationException`）。该异常落入外层 `catch (Exception)`（L89）→ 删除 temp 后以 `IOException` 重抛，L87 的 `REPLACE_EXISTING` 回退永不触发，与方法 Javadoc（L69「If atomic move is unsupported, REPLACE_EXISTING is used」）承诺相反。 | 改为 `catch (AtomicMoveNotSupportedException ex)`（或 `FileSystemException`）后再走 `REPLACE_EXISTING` 回退；并加测试覆盖该分支。 | 新增 CA-017 |
| Medium | §2 | `db/postgres/PostgresSchemaIsolation.java:59-65`<br>`db/mysql/MySqlSchemaIsolation.java:42-48` | public `setSearchPath` 将 `schema` 直接拼入 DDL（`SET search_path TO <schema>, pg_catalog` / `USE <schema>`），**未**调用 `isSafeIdentifier`，与同类 `teardown`（PG L45 / MySQL L34 已校验）行为不一致。当前调用方（`FileRunner`）仅传入 `prepare()` 生成的 UUID 名（实践安全），但 public 入口缺纵深防御。 | 在 `setSearchPath` 入口对 `schema` 调用 `isSafeIdentifier`，与 `teardown` 对齐；或在 Javadoc 明确前置条件「调用方须保证标识符安全」。 | 新增 CA-018 |
| Medium | §2 | `cli/CliArgumentParser.java:96-101` | `requireValue` 把任何 `startsWith("-")` 的 token 当作「missing value」（L97）。导致 `--password -secret`、`--user -name`、`--env-file -path` 等以 `-` 开头的合法值被拒。密码以 `-` 开头属合理输入。 | 对接受任意值的选项（至少 `--password`、`--user`、`--env-file`、`--url`）允许 `-` 开头的值；或支持 `--` 终止选项解析。 | 新增 CA-019 |
| Medium | §2 / §9 | `cli/Main.java:107-122` | `run(...)` 顶层仅 `catch (UsageException → exit 2)`（L118-121）。类 Javadoc（L15-16）承诺 exit 2 覆盖「connection / fatal errors」，但任何非 `UsageException` 的异常（如逃逸的 NPE、未预期的 RuntimeException）会传播出 `run`，在 `main`（L27）以未捕获异常退出，退出码不受控（JVM 通常 1）。正常错误路径已在 `CliSession` / `FileRunner` 内部映射为 FileOutcome，故实际触发概率低。 | 顶层补兜底 `catch (Throwable)` 映射到稳定退出码（建议 2），并在 `err` 打印简短摘要（经脱敏）；同时确认 `CliSession.execute` 内部对所有致命路径均映射为预期码。 | 新增 CA-020 |
| Low | §7 | `db/AbstractJdbcExecutor.java:134-138` | `summarize` 原样返回 `SQLException.getMessage().strip()`（L136-137），但 Javadoc（L134）声称「carries no connection string or credentials」——该保证仅是注释，本层未做任何脱敏。实际凭据保护在 CLI 输出层（`CredentialRedaction`，对应 CA-002 / CA-009）生效。层内 Javadoc 误导。 | 弱化 Javadoc 为「raw driver message；凭据脱敏由 CLI 输出层负责」，或在本层增加长度/字符净化；不要给读者「本层已脱敏」的错误印象。 | 新增 CA-021（与 CA-009 相关） |
| Low | §4 | `model/QueryRecord.java:52-60`<br>`normalize/ResultComparer.java:86-94` | `columnSeparator` 校验（空串 + 含空白 + 相同异常消息）在两处逐字符重复。 | 抽取共享校验静态方法（如 `ColumnSeparators.validate(Optional<String>)`），两处复用。 | 新增 CA-022 |
| Low | §4 | `db/postgres/PostgresSchemaIsolation.java:67-69` & `db/mysql/MySqlSchemaIsolation.java:50-52` | `isSafeIdentifier`（正则 `[a-z][a-z0-9_]*`）与 `prepare` 中 schema 名生成（`"ggtest_" + UUID…replace("-","").toLowerCase()`）在 PG / MySQL 两类逐字符相同，易单边漂移（CA-018 的 `setSearchPath` 缺校验即此类风险的症状）。 | 抽取到 `com.ggtest.db` 下共享工具（如 `SchemaNames.generate()` / `isSafe(...)`），隔离类仅保留 DDL 差异。 | 并入 CA-018 建议 |
| Low | §2 | `cli/RuntimeConfigResolver.java:158-180` | `resolveHashThreshold` / `parseHashThreshold` 未校验非负范围；`<= 0` 语义为「禁用 hash」但负值不明确。CA-011 open，不变。 | 解析处 `if (value < 0) throw new UsageException(...)` 或文档标明负值等价禁用。 | CA-011 |
| Low | §4 | `normalize/ResultComparer.java:154-190` | `diffOps` 经典 LCS 全表 `O(n·m)` 时空，仅用于失败 diff 呈现。CA-007 accepted，不变。 | 超大失败集可换滚动数组或 Myers；通过路径不受影响。 | CA-007 |

## 关注项（Known Issue / Tech Debt / 优化）

| 类型 | 级别 | 位置 | 描述 | 影响 | 建议下一步 | 登记册 |
|---|---|---|---|---|---|---|
| Tech Debt | Low | `cli/OverrideWriter.java:147-157` vs `normalize/ExpectedResultExpander.java:95-111` | `splitOnEol` 与 `splitLiteral` 为同族「按字面分隔符切分」手写实现（行为略有差异：`splitLiteral` 对 null/空 delim 抛 `IllegalArgumentException`，`splitOnEol` 无此校验） | 两份实现各自演化，行为可能漂移 | 抽取共享 util（如 `Strings.splitLiteral(text, delim)`）两侧复用 | CA-012（不变） |
| Tech Debt | Low | `cli/DotEnvLoader.java:24-29` | `WHITELIST` 硬编码 `Set.of(...)` | 新增配置键需重新编译 | 当前 5 个键规模可接受；按需改外部配置 | CA-013（不变） |
| Tech Debt | Info | `parser/SqlLogicTestParser.java:627-643`（`LineBuffer.splitPreserveAllLines`） | 每次解析将整个文件内容拷贝到 `ArrayList<String>` | 大文件（>100MB）可能 OOM；sqllogictest 文件通常很小 | 仅在文件极大时关注；可改流式读取 | CA-014（不变） |
| Tech Debt | Low | `normalize/ResultComparer.java:131-134`（`splitExpectedLines`） | 仅为包内测试保留的转发方法（`ExpectedResultExpander.physicalLines`） | 扩大包级 API 面 | 测试稳定后移除或改测 `physicalLines` | — |
| 优化 | Low | `runner/SqlLogicTestRunner.java:160, 408-415` | `run()` 起始即 `getDefaultExecutorName(executors)` → `computeIfAbsent("", factory)` 触发默认执行器构造；纯命名连接（`conn=`）文件亦然。PG/MySQL 因 `""` 连接已在 `prepare` 打开故无新连接，SQLite 则会多开一个内存库 | 无用对象/连接开销，范围窄 | 惰性创建默认执行器；或 engine name 由工厂元数据提供 | — |
| 优化 | Info | `runner/SqlLogicTestRunner.java:403-405`（`matchesEngine`） | `consumePendingSkip` 循环内每次对 `dbName` 与 `engineName` 调 `toLowerCase(Locale.ROOT)`；`engineName` 在 `FileState` 内不变 | 每 skipif/onlyif 行开销可忽略 | 缓存 `engineName` 小写形式 | — |
| 优化 | Low | `normalize/ResultHasher.java:75-81`（`toLowerHex`） | 每字节 `String.format` 略低效 | 微性能 | 改 `HexFormat.of()` 或查表 | — |
| 优化 | Info | `cli/CliOptions.java:77-78`（`toString`） | `password` 已脱敏（`Optional.of("***")`，L80），URL userinfo 经 `CredentialRedaction` 脱敏（L76）；但 `user` 明文输出 | 敏感度低于密码；同处凭据上下文 | 视威胁模型决定是否同样以 `***` 占位 | — |

## 登记册核对

| ID | 原状态 | 审计结论 | 建议 |
|---|---|---|---|
| CA-001~CA-006 | resolved | 已确认 resolved | none |
| CA-007 | accepted | 仍存在；不变（`ResultComparer.diffOps` LCS 全表） | none |
| CA-008 | accepted | 仍存在且已文档化（`ValueNormalizer` 非整型→`"0"`、非 real→`"0.000"`，类/方法 Javadoc 标注 Spec 对齐） | none |
| CA-009 | open | 仍存在；不变（`CredentialRedaction` 仅覆盖 `://user:pass@`） | none |
| CA-010 | resolved | 已确认 resolved（FileRunner 已拆 ConnectionFactory + OverrideCoordinator） | none |
| CA-011 | open | 仍存在；不变 | none |
| CA-012 | open | 仍存在；不变 | none |
| CA-013 | open | 仍存在；不变 | none |
| CA-014 | open | 仍存在；不变 | none |
| CA-015 | resolved | 已确认 resolved（适配器已删除） | none |
| CA-016 | resolved | 已确认 resolved（token 索引定位属性边界） | none |
| 新增 CA-017 | — | Medium §5/§2 OverrideWriter 原子移动回退死代码 | 见 Findings |
| 新增 CA-018 | — | Medium §2 setSearchPath 缺标识符校验（+ Low §4 isSafeIdentifier/schema 名生成重复） | 见 Findings |
| 新增 CA-019 | — | Medium §2 CLI 拒绝以 `-` 开头的值 | 见 Findings |
| 新增 CA-020 | — | Medium §2/§9 Main 缺顶层兜底异常映射 | 见 Findings |
| 新增 CA-021 | — | Low §7 AbstractJdbcExecutor.summarize Javadoc 误导（层内未脱敏） | 见 Findings |
| 新增 CA-022 | — | Low §4 columnSeparator 校验重复 | 见 Findings |

> 登记册由人工或审计后整理维护（见 `code-audit-register.md` 顶部说明）；本次审计只读，未改登记册。建议将上述新增 / 状态变更整理入册。

## 覆盖说明
- 已读：`workflow/agents/standards/code-audit.md`；`workflow/audit/register.md`；`src/main/java/**/*.java`（51 个全部）
  - 直接精读：cli（FileRunner / CliSession / ParallelExecutor / ConnectionFactory / OverrideCoordinator / TestFileCollector / RuntimeConfigResolver / CredentialRedaction / DotEnvLoader / CliArgumentParser / Main / OverrideWriter）、db（AbstractJdbcExecutor / PostgresSchemaIsolation / MySqlSchemaIsolation / MySqlJdbcExecutor）、parser（SqlLogicTestParser 含 LineBuffer）、model（QueryRecord）、normalize（ResultComparer 片段）
  - 包级深读（取证后逐条复核）：normalize 全量 + model 全量；db 全量；runner 全量 + cli 余量
- 分层（§1）：`model` ← `parser` / `normalize` / `db` / `runner` ← `cli`，依赖方向正确；`SqlTestRecord` sealed interface 提供编译期穷举。引擎子包单向依赖 `com.ggtest.db` 抽象，无循环。
- 并发（§2）：`--parallel` 下 `SqlLogicTestParser` 跨工作线程共享——已确认其无实例可变字段，每次 `parse` 新建 `LineBuffer`，线程安全；每个 worker 自建 `FileRunner`（独立连接），状态行由主线程按输入序串行打印。`SqlLogicTestRunner` 类 Javadoc 已声明「not thread-safe」，单连接串行使用契约仅写在 `SqliteJdbcExecutor`（见 §2 关注项，建议上移到接口/基类 Javadoc）。
- 正确性（§2）：CA-016 已修复确认；新发现 setSearchPath 校验缺口、CLI `-` 值、Main 兜底。
- 资源（§5）：FileRunner 多连接模式在 `finally` 统一关闭所有连接 + PG/MySQL schema teardown，正确；`Files.walk`（TestFileCollector）、Statement（AbstractJdbcExecutor）均 try-with-resources；`OverrideWriter` temp+rename 失败删 temp 保原文件，正确（除 CA-017 回退死代码外）。
- 敏感信息（§7）：源码无硬编码凭据；`CliOptions.toString()` 密码脱敏正确；诊断输出经 `CredentialRedaction`；CA-009 仍适用（URL 查询参数 / 非 URL 形式凭据脱敏缺口）；CA-021（summarize Javadoc 误导）为层内文档问题，实际凭据保护在输出层。
- 测试友好（§6）：`DatabaseExecutor` 接口 + `executeStatement/executeQuery(sql, timeoutMs)` 默认方法向后兼容；`FakeDatabaseExecutor` 存在；`Main.run` 多层可注入重载。
- 范围内未发现 `TODO` / `FIXME` / `HACK` / `XXX` / `TECHDEBT` 标记（normalize / model / db / runner / cli 均无匹配）。
- 未覆盖条款：无专项 `code-audit-*.md`；§3 嵌套深度未做逐文件圈复杂度计量；未加载 `quality.md` / `git.md` / `documentation.md`（用户未点名）。`src/test/**`（31 个测试文件）未纳入本次违规取证（标准 §6 对测试源码的覆盖为可选，本次聚焦 `src/main`）。
