# 代码质量审计报告

- 日期：2026-08-11
- 执行：code-audit skill（只读，未改 `src/`）
- 基准上一次审计：2026-07-26（`workflow/audit/2026-07-26-src.md`）

## 范围
- 代码：`src/main/java` 45 个生产源文件（model 11 / parser 2 / normalize 5 / db 6 / runner 4 / cli 17）
- 标准：`workflow/agents/standards/code-audit.md`（无新增 `code-audit-<topic>.md` 专项）
- 登记册：`workflow/audit/register.md`（8 条；CA-001~CA-006 已 resolved；CA-007, CA-008 accepted）
- 抽查 `pom.xml`、`.env.example`、`.gitignore` 作 §7 佐证

## 摘要
- Findings — Critical: 0 | High: 0 | Medium: 2 | Low: 3 | Info: 0
- 关注项 — Known Issue: 0 | Tech Debt: 2 | 优化: 1
- 整体质量良好。上次审计的全部 Medium/High 违规均已修复（重复执行器已抽取、凭据脱敏已落地、常量已收敛、PG teardown 已修复、会话边界已拆分）。架构分层清晰，资源管理规范。

## 上次审计跟踪

| 违规/关注 | 状态 | 验证 |
|---|---|---|
| CA-001 JDBC 执行器重复 (M) | **resolved** | `AbstractJdbcExecutor` 已抽出共享基类；`SqliteJdbcExecutor`/`PostgresJdbcExecutor` 仅声明 engine name + fatal markers |
| CA-002 凭据脱敏未落地 (M) | **resolved** | `CredentialRedaction` 类已创建；`FileRunner.sanitize()` 对所有错误输出做 URL userinfo 脱敏 + 密码字面替换 |
| CA-003 CliSession 过大 | **resolved** | `FileRunner` / `ReportWriter` 已拆分；`CliSession` 仅编排多文件循环 |
| CA-004 hash-threshold 双定义 | **resolved** | 收敛到 `ResultComparer.DEFAULT_HASH_THRESHOLD`；`RuntimeConfigResolver` 引用该常量 |
| CA-005 column-separator 双定义 | **resolved** | column separator 已改为 per-query（`QueryRecord.columnSeparator`），不再有全局常量漂移 |
| CA-006 PG teardown 双路径 | **resolved** | `FileRunner.runPostgresFile` 统一走 `finally`；`schema != null` 时才 teardown |
| CA-007 LCS O(n*m) | **accepted** (不变) | `ResultComparer.diffOps` 未变 |
| CA-008 ValueNormalizer 非法 I/R | **accepted** (不变) | Javadoc 已标明语义 |

## Findings（违规）

| 级别 | 标准 | 位置 | 问题 | 建议 | 修复跟踪 |
|---|---|---|---|---|---|
| Medium | §7 | `CredentialRedaction.java:9-18`<br>`FileRunner.java:207-209` | `redactUrlUserInfo` 仅匹配 `scheme://user:pass@host` 格式；若凭据嵌在 JDBC URL 的查询参数中（如 `?password=secret`）或驱动异常消息回显非 URL 形式的凭据，脱敏无法覆盖。若用户仅通过 URL 嵌凭据而不单独配置 `--password`/`GGTEST_PASSWORD`，`redactMessage` 中的密码字面量替换也不会生效。 | 对 `sanitize` 增加通用敏感参数名模式匹配（如 `password=`, `pwd=`），或文档明确告知用户：凭据须通过 `--password`/`GGTEST_PASSWORD` 单独提供才能确保脱敏。 | CA-009 |
| Medium | §1 | `FileRunner.java:26-210` | `FileRunner` 同时承担 JDBC 连接管理（`openConnection`）、引擎路由（`runSqliteFile`/`runPostgresFile`）、PG schema 隔离、override 写回编排、parser 调用编排、sanitize。虽已从 `CliSession` 拆出，但自身仍为 210 行的「上帝对象」。 | 按职责进一步拆分：连接工厂（`ConnectionFactory`）、引擎适配（多态 `FileRunner` 或策略模式）、override 写回委托给独立的 `OverrideCoordinator`。 | CA-010 |
| Low | §4 | `ResultComparer.java:147-170` | `diffOps` 使用经典 LCS 表 `O(n*m)` 时空。已在登记册 CA-007 标记 accepted。 | 超大结果集失败时可能陡增；建议后续对行数超过阈值（如 500）的 diff 改用 Myers 或截断。 | CA-007（不变） |
| Low | §2 | `RuntimeConfigResolver.java:154-168` | `resolveHashThreshold` 未验证阈值有效范围（如非负）。`hashThreshold <= 0` 在 `renderActual` 中语义为「禁用 hash」，但 `-1` 作为阈值不明确。 | 在解析处增加 `if (value < 0) throw new UsageException(...)` 或文档标明负值等价于禁用。 | CA-011 |
| Low | §4 | `ExpectedResultExpander.java:97-106` vs `OverrideWriter.java:140-150` | `splitLiteral`（按字面分隔符）和 `splitOnEol`（按 EOL）实为同族的字符串分割工具，在 normalize 和 cli 两个包中各自维护。 | 可抽取到共享工具类中；当前重复量不大（~10 行），可作为低优先级代码清理。 | CA-012 |

## 关注项（Known Issue / Tech Debt / 优化）

| 类型 | 级别 | 位置 | 描述 | 影响或收益 | 建议下一步 | 登记册 |
|---|---|---|---|---|---|---|
| Tech Debt | Low | `DotEnvLoader.java:24-29` | `WHITELIST` 硬编码为 `Set.of(...)`；添加新环境变量需修改源码。 | 新增配置键需重新编译；不影响已有功能。 | 可改为外部配置或注解驱动；当前 5 个键的规模可接受，按需改。 | CA-013 |
| Tech Debt | Info | `SqlLogicTestParser.java`（`LineBuffer.splitPreserveAllLines`） | 内部类 `LineBuffer` 在调用 `from()` 时拷贝整个文件内容到 `ArrayList<String>`。大文件（>100MB）可能 OOM。 | sqllogictest 文件通常较小，实际风险低。 | 仅在文件极大时关注；可改为流式读取但会显著增加实现复杂度。 | CA-014 |
| 优化 | Low | `SqlLogicTestRunner.java:341-342` | `matchesEngine` 每次比较都调用 `dbName.toLowerCase(Locale.ROOT)`，已在 `consumePendingSkip` 中遍历调用。可以为 `SkipIfRecord`/`OnlyIfRecord` 构造时预归一化。 | 每个文件的 skipif/onlyif 行通常很少（<10），开销可忽略。 | 无需立即处理；若未来语料文件出现大量条件指令时再优化。 | — |

## 登记册核对

| ID | 原状态 | 审计结论 | 建议 |
|---|---|---|---|
| CA-001 | resolved | 已确认 resolved | none |
| CA-002 | resolved | 已确认 resolved | none |
| CA-003 | resolved | 已确认 resolved | none |
| CA-004 | resolved | 已确认 resolved | none |
| CA-005 | resolved | 已确认 resolved | none |
| CA-006 | resolved | 已确认 resolved | none |
| CA-007 | accepted | 仍存在；保持不变 | none |
| CA-008 | accepted | 仍存在；保持不变 | none |
| 新增 CA-009 | — | Medium §7 脱敏覆盖 | 见 Findings 表 |
| 新增 CA-010 | — | Medium §1 FileRunner 职责过重 | 见 Findings 表 |
| 新增 CA-011 | — | Low §2 hash-threshold 范围未校验 | 见 Findings 表 |
| 新增 CA-012 | — | Low §4 字符串分割重复 | 见 Findings 表 |
| 新增 CA-013 | — | Low Tech Debt DotEnvLoader 白名单硬编码 | 见关注项表 |
| 新增 CA-014 | — | Info Tech Debt LineBuffer 全量读入 | 见关注项表 |

## 覆盖说明
- 已读：`workflow/agents/standards/code-audit.md`；`workflow/audit/register.md`；`src/main/java/**/*.java`（45 个全部）
- 分层（§1）：`model` ← `parser`/`normalize`/`db`/`runner` ← `cli`，依赖方向正确；`SqlTestRecord` sealed interface 提供编译期穷举保证。
- 正确性/资源（§2/§5）：Statement/ResultSet/Connection/Stream 普遍 try-with-resources；`FatalDatabaseException` 与业务失败分流清晰；null 处理合理（normalizer 0/0.000 默认值、`Objects.requireNonNull`）。
- 并发（§2）：runner 文档约定单连接串行；`SqlLogicTestRunner` 实例重用但不线程安全（注释已声明）；未见错误的共享可变静态状态。
- 敏感信息（§7）：源码无硬编码凭据；`.env.example` 为模板值；`.env` 在 `.gitignore` 中排除；`CredentialRedaction` 在诊断输出路径上生效；`CliOptions.toString()` 脱敏密码。`AbstractJdbcExecutor.summarize()` 可能暴露 SQLException 原始消息中的连接详情，但所有消费方均经 `sanitize` 处理。
- 测试友好（§6）：`FakeDatabaseExecutor` 存在；`Main.run` 有多层可注入重载；`DatabaseExecutor` 接口隔离。
- 依赖（pom.xml）：`sqlite-jdbc 3.53.2.0`、`postgresql 42.7.13`、`junit-jupiter 5.10.2`，均为当前稳定版本。
- 范围内未发现 `TODO` / `FIXME` / `HACK` / `XXX` / `TECHDEBT` 标记。
- 未覆盖条款：无专项 `code-audit-*.md`；§3 嵌套深度未做逐文件圈复杂度计量；未加载 `quality.md` / `git.md` / `documentation.md`（用户未点名）。
