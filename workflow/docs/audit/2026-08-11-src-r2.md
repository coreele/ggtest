# 代码质量审计报告

- 日期：2026-08-11（第二轮）
- 执行：code-audit（只读，未改 `src/`）
- 基准上一次审计：2026-08-11 第一轮（`workflow/docs/audit/2026-08-11-src.md`）

## 范围
- 代码：`src/main/java` 51 个生产源文件（上次 45；新增 ConnectionFactory / EngineAdapter / SqliteAdapter / PostgresAdapter / OverrideCoordinator）
- 标准：`workflow/docs/standards/code-audit.md`（无 `code-audit-<topic>.md` 专项）
- 登记册：`workflow/docs/standards/code-audit-register.md`（14 条）
- 自上次审计后的重大变更：`separator=<delim>` key=value 语法、`timeout=<ms>` 属性、`conn=<name>` 多连接、FileRunner 重构、`--override` row-wise 修复

## 摘要
- Findings — Critical: 0 | High: 0 | Medium: 2 | Low: 2 | Info: 0
- 关注项 — Known Issue: 1 | Tech Debt: 1
- 整体质量良好。上次审计的 CA-010（FileRunner 职责过重）已通过 ConnectionFactory / OverrideCoordinator 拆分解决。新发现 2 个 Medium 问题：死代码适配器类和 statement error 消息提取遗漏 conn= 属性。

## 登记册核对

| ID | 原状态 | 审计结论 | 建议 |
|---|---|---|---|
| CA-001~CA-006 | resolved | 已确认 resolved | none |
| CA-007 | accepted | 仍存在；不变 | none |
| CA-008 | accepted | 仍存在；不变 | none |
| CA-009 | open | 仍存在；不变 | none |
| CA-010 | open | **→ resolved**：FileRunner 已拆分为 ConnectionFactory + OverrideCoordinator，179 行（原 210），职责清晰 | 标记 resolved |
| CA-011 | open | 仍存在；不变 | none |
| CA-012 | open | 仍存在；不变 | none |
| CA-013 | open | 仍存在；不变 | none |
| CA-014 | open | 仍存在；不变 | none |
| 新增 CA-015 | — | Medium §1 死代码：EngineAdapter / SqliteAdapter / PostgresAdapter | 见 Findings |
| 新增 CA-016 | — | Medium §2 statement error 消息提取遗漏 conn= | 见 Findings |

## Findings（违规）

| 级别 | 标准 | 位置 | 问题 | 建议 | 修复跟踪 |
|---|---|---|---|---|---|
| Medium | §1 | `cli/EngineAdapter.java`<br>`cli/SqliteAdapter.java`<br>`cli/PostgresAdapter.java`（共 ~118 行） | 死代码。这三个类在 `refactor-filerunner-responsibilities` 中创建，但 FileRunner 为 `conn=<name>` 多连接完全重写后不再使用适配器模式。全仓库无其他引用。 | 删除这三个文件；或若未来计划恢复适配器模式则标注 `@Deprecated` 并说明。 | CA-015 |
| Medium | §2 | `parser/SqlLogicTestParser.java:169-172` | statement error 消息提取仅查找 `" timeout="` 和 `"\t"` 作为属性边界。`conn=` 属性不被识别，导致 `statement error division by zero conn=c1` 的 `expectedErrorMsg` 被错误提取为 `"division by zero conn=c1"` 而非 `"division by zero"`。 | 改用 token 索引（`firstAttrIndex`）在原始行中定位消息结束位置，而非字符串搜索 `" timeout="`。或直接用 `String.join(" ", tokens[2..firstAttrIndex])` 构建消息。 | CA-016 |
| Low | §4 | `ResultComparer.java:147-170` | `diffOps` 使用经典 LCS 表 `O(n*m)`。CA-007 accepted。 | 不变。 | CA-007 |
| Low | §2 | `RuntimeConfigResolver.java:154-168` | hash-threshold 未验证非负范围。CA-011 open。 | 不变。 | CA-011 |

## 关注项

| 类型 | 级别 | 位置 | 描述 | 影响 | 建议 | 登记册 |
|---|---|---|---|---|---|---|
| Tech Debt | Low | `cli/FileRunner.java:60` | `String[] fileSchemaHolder = {null}` 绕过 lambda effectively-final 约束。可读性略差。 | 仅风格问题，行为正确。 | 可提取为单独方法返回 `(Connection, String)` pair。 | — |

## 覆盖说明
- 已读：`code-audit.md`；`code-audit-register.md`；`src/main/java/**/*.java`（51 个全部，重点精读 FileRunner / SqlLogicTestRunner / SqlLogicTestParser / AbstractJdbcExecutor / PostgresSchemaIsolation / OverrideWriter）
- 分层（§1）：`model` ← `parser`/`normalize`/`db`/`runner` ← `cli`，依赖方向正确。**死代码例外**：EngineAdapter/SqliteAdapter/PostgresAdapter 不在任何调用链上。
- 正确性（§2）：statement error + conn= 属性的 `expectedErrorMsg` 提取有误（CA-016）。
- 资源（§5）：FileRunner 多连接模式在 `finally` 中统一关闭所有连接 + PG schema teardown，正确。
- 敏感信息（§7）：不变，CA-009 仍适用。
- 测试友好（§6）：`DatabaseExecutor` 新增 `executeStatement(sql, timeoutMs)` / `executeQuery(sql, timeoutMs)` default 方法，向后兼容。
- 范围内未发现 `TODO`/`FIXME`/`HACK`/`XXX`/`TECHDEBT` 标记。
- 未覆盖：无专项 `code-audit-*.md`；§3 嵌套深度未做逐文件圈复杂度计量。
