# 代码质量审计报告

- 日期：2026-07-26
- 执行：code-audit skill（只读，未改 `src/`）

## 范围
- 代码：`src/`（`src/main/java` 40 个生产源文件；抽查 `src/test/java` 作 §6/§8 佐证）
- 标准：`workflow/workflow/docs/standards/code-audit.md`（无 `code-audit-<topic>.md` 专项）
- 登记册：`workflow/workflow/docs/standards/code-audit-register.md`（存在；登记条目表为空）

## 摘要
- Findings — Critical: 0 | High: 0 | Medium: 2 | Low: 2 | Info: 0
- 关注项 — Known Issue: 0 | Tech Debt: 3 | 优化: 2
- 整体质量良好：分层清晰、资源管理规范、错误路径可观察；主要问题是 JDBC 执行器重复与凭据脱敏控制点未真正落地。

## Findings（违规）

| 级别 | 标准 | 位置 | 问题 | 建议 | 修复跟踪 |
|---|---|---|---|---|---|
| Medium | `workflow/workflow/docs/standards/code-audit.md`§4 | `src/main/java/com/ggtest/db/sqlite/SqliteJdbcExecutor.java` 与 `.../postgres/PostgresJdbcExecutor.java`（全文近乎同构，约 L33–144） | 两引擎执行器在 `executeStatement` / `executeQuery` / `readRows` / `isFatal` / `summarize` 等路径上明显可抽取的重复逻辑，后续修致命判定或行读取时易漂移。 | 抽取共享基类或 package-private helper（fatal 判定策略可注入引擎差异）；保留引擎专属 `ENGINE_NAME` 与 marker 列表。 | 进行中 → `fix-jdbc-executor-dedup` / CA-001 |
| Medium | `workflow/workflow/docs/standards/code-audit.md`§7 | `src/main/java/com/ggtest/cli/CliSession.java:392-395` | `sanitize` 注释写明「Strips accidental credential echoes; never logs passwords」，实现仅 `strip()`，未脱敏。连接失败等路径会把 JDBC 消息原样写入 stdout/stderr；若驱动/URL 回显含凭据片段，控制点失效。 | 对消息做 URL userinfo / 已知 password 字面量脱敏，或收窄注释并改为显式「仅 strip、依赖上游不回显」。 | 进行中 → `fix-cli-credential-redaction` / CA-002 |
| Low | `workflow/workflow/docs/standards/code-audit.md`§7 | `src/main/java/com/ggtest/cli/CliOptions.java:37-54` | `toString` 已将 `password` 字段打成 `***`，但完整 `url` 原样输出；若 JDBC URL 内嵌 `user:pass@`，诊断打印仍可能泄露。 | 对 `url` 做 userinfo 脱敏后再拼 `toString`；补测试覆盖嵌入凭据的 URL。 | 进行中 → `fix-cli-credential-redaction` / CA-002 |
| Low | `workflow/workflow/docs/standards/code-audit.md`§4 | `CliArgumentParser.java:21` 与 `ResultComparer.java:23` | `DEFAULT_HASH_THRESHOLD = 8` 双处定义；CLI 默认与 normalize 默认可独立漂移。 | 收敛为单一权威常量（例如 normalize 导出，CLI 引用），或文档标明二者必须同步并加契约测试。 | 进行中 → `fix-shared-defaults` / CA-004 |

## 关注项（Known Issue / Tech Debt / 优化）

| 类型 | 级别 | 位置 | 描述 | 影响或收益 | 建议下一步 | 登记册 |
|---|---|---|---|---|---|---|
| Tech Debt | Medium | `CliSession.java`（约 420 行；`execute` / `runOneFile` / `runPostgresFile` / 报告格式化） | CLI 会话同时承担文件编排、JDBC 连接生命周期、PG schema 隔离、报告渲染与脱敏。 | 改报告或换引擎时牵动面大，单测需整条编排。 | 拆出 `FileRunner` / `ReportWriter` 边界；隔离层保持 `DatabaseExecutor`。 | CA-003 → `refactor-cli-session-boundaries`（等依赖） |
| Tech Debt | Low | `CliSession.java:187-226` | PG teardown：try 内失败返回 `hardFailure` 时 `schema` 未置空，`finally` 会再 teardown 一次。 | 二次 DROP 通常无害，但错误日志可能重复，掩盖首次失败。 | 失败路径统一走 finally，或成功后清 `schema` 并避免双路径。 | CA-006 → `fix-pg-teardown-once` |
| Tech Debt | Low | `SqlLogicTestParser.java:33` 与 `ResultComparer.java:26` | `DEFAULT_COLUMN_SEPARATOR`（空格）双处定义。 | 与 hash-threshold 类似的常量漂移风险。 | 与阈值常量一并收敛。 | CA-005 → `fix-shared-defaults` |
| 优化 | Low | `ResultComparer.java:160-195` | 失败 diff 用经典 LCS 表 `O(n*m)` 时空。 | 超大结果集失败报告时内存/CPU 可能陡增；通过路径不受影响。 | 大 diff 改 Myers/限长摘要；或对超长视图只报首尾。 | CA-007 accepted（本批不做） |
| 优化 | Info | `ValueNormalizer.java:32-47` | 非法 I/R 输入归一为 `"0"` / `"0.000"`（测试已覆盖，对齐 sqllogictest）。 | 非缺陷；新人易误判为吞错。 | 在方法 Javadoc 标明 Spec 语义；无需改行为。 | CA-008 accepted |

## 修复编排（Manager 2026-07-26）

用户授权自行决断拆分与门禁；**合入 main 须审批**。工作项：`fix-jdbc-executor-dedup`、`fix-cli-credential-redaction`、`fix-shared-defaults`、`fix-pg-teardown-once`、`refactor-cli-session-boundaries`（后置）。

范围内未发现 `TODO` / `FIXME` / `HACK` / `XXX` / `TECHDEBT` 标记。

## 登记册核对

| ID | 原状态 | 审计结论 | 建议 |
|---|---|---|---|
| 无 → CA-001..CA-008 | — | 审计时登记条目表为空 | **已入库**（Manager 2026-07-26）：见 `workflow/workflow/docs/standards/code-audit-register.md`；修复跟踪见上表「修复跟踪/登记册」列 |

## 覆盖说明
- 已读：`workflow/workflow/docs/standards/code-audit.md`；`workflow/workflow/docs/standards/code-audit-register.md`；`src/main/java/com/ggtest/**/*.java`（40）；重点精读 parser / runner / cli / db / normalize；抽查 normalize/cli 相关测试。
- 分层（§1）：`model` ← `parser`/`normalize`/`db`/`runner` ← `cli`，未见下层依赖 CLI；runner 经 `DatabaseExecutor` 隔离引擎。
- 正确性/资源（§2/§5）：业务失败与 fatal 分流清晰；Statement/ResultSet/Connection/Files.walk 普遍 try-with-resources。
- 并发（§2）：runner/executor 文档约定单连接串行；未见错误的共享可变静态状态。
- 敏感信息（§7）：源码无硬编码生产凭据；测试中 `GGTEST_PASSWORD="secret"` 为夹具字面量。未回显完整秘密。
- 测试友好（§6）：存在 `FakeDatabaseExecutor`、可注入 `Main.run`；未发现恒真空测。
- 未覆盖条款：无专项 `code-audit-*.md`；§3 嵌套深度、§6「测试破坏封装」未做逐文件圈复杂度计量，属抽样结论。
- 未加载：`quality.md` / `git.md` / `documentation.md`（用户未点名）。
